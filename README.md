
  # Tend

  Tend is a low-pressure, gamified CGM (continuous glucose monitor) companion-pet app. Caring for a virtual pet is paired with lightweight glucose logging and trend review, with an AI chat companion for supportive, non-medical conversation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
